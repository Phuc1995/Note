ps -ef | grep defunct  tim cac tien trinh rac
ps -ef | grep nhoangp+ | grep jboss | grep defunct | awk '{print $3}' | xargs kill -9  xoa jboss rac

|grep : loc file

cp \path_can _opy .(cp vi tri hien tai)

Tim ten trong linux:
-grep -rnw . -e "tenfilecantim"
- /"String can tim", n next, Shit n: come back 

 
